# 内存模型和命名空间
C++在内存中存储数据方面提供了很多种存储方式。
- 可以选择数据保留在内存中的时间长度（存储持续性）以及程序的哪一部分可以访问数据（作用域和链接等）
- 可以使用 `new` 来动态分配内存，而定位 `new` 运算符提供了这种技术的一个变种
- 名称空间是另一种控制访问权的方式
## 单独编译
正常的程序应当包含以下三个部分：
- 头文件：包含结构声明和使用这些结构的函数的与原型
- 源代码文件：包含与结构有关的函数的代码
- 源代码文件：包含调用与结构相关的函数的代码
    **不要将函数定义和变量声明放到头文件中**，可能会造成重复定义或者其他错误发生

头文件中通常包含以下内容：
- 函数原型
- 使用 `#define` 或 `const` 定义的符号常量
- 结构声明
- 类声明
- 模板声明
- 内联函数

将结构声明放在头文件中是可以的，因为它们不创建变量，而只是在源代码文件中声明结构变量时，告诉编译器如何创建该结构变量，模板声明也是，因为它指示编译如何生成与源代码中的函数调用相匹配的函数定义，被声明为 `const` 的
## 存储持续性、作用域和连接性
### 作用域和链接
### 自动存储持续性
### 静态持续变量
### 静态持续性、外部链接
### 静态持续性、内部链接
### 静态存储持续性、无连接性
### 说明符和限定符
### 函数和链接性
### 语言链接性
### 存储方案和动态分配
## 名称空间
### 传统的C++名称空间
### 新的名称空间特性
### 名称空间示例
### 名称空间及其前途
## 总结
## 复习题
## 编程练习
